// Build a simple loop, inside a function, that takes an
// array and prints the value of the array to the console.

function printSiblings(arr) {
  for (let sibling = 0; sibling <= siblings.length - 1; sibling++) {
    console.log(arr[sibling]);
  }
}

let siblings = ["Elpe", "Sonia", "Stevoo", "Glady", "Growel"];
printSiblings(siblings);
