// Build as simple loop, that prints numbers to the console.
// from 0 to the length of the Array.

let siblings = ["Elpe", "Sonia", "Stevoo", "Glady", "Growel"];

for (let sibling = 0; sibling <= siblings.length - 1; sibling++) {
  console.log(sibling);
}
